import {  } from "../actions"; 

const INITIAL_STATE = {
    user = {}


}

const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case v:
            
            break;
    
        default:
            break;
    }
  
    
}

export default reducer;